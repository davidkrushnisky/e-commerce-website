@model IEnumerable<ECommercePlateform.Models.Product>

@{
    ViewData["Title"] = "All Products";
}

<h1>@ViewData["Title"]</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pictures)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <img src="data:image;base64,@System.Convert.ToBase64String(item.Pictures)" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@
@*MAKE IT A PARTIAL VIEW OR COMPONENT..*@
<div class="dropdown">
    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
        Filter By Category
    </button>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
        <a  class="btn btn-primary">View Details</a>
        <li><a class="dropdown-item" asp-controller="Products" asp-action="ProductsOfCategory" asp-route-id=1>Headphones</a></li>
        <li><a class="dropdown-item" asp-controller="Products" asp-action="ProductsOfCategory" asp-route-id=2>Keyboards</a></li>
        <li><a class="dropdown-item" asp-controller="Products" asp-action="ProductsOfCategory" asp-route-id=3>Monitors</a></li>
        <li><a class="dropdown-item" asp-controller="Products" asp-action="Index" asp-route-id=3>View All</a></li>
    </ul>
</div>
<div class="container-fluid">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-sm-3">
                <div class="card h-100" style="width: 18rem;">
                    <img src="data:image;base64,@System.Convert.ToBase64String(item.Pictures)" />
                    <div class="card-body">
                        <h5 class="card-title">@item.Name)</h5>
                        <p class="card-text">@item.Description)</p>
                        <a asp-controller="Products" asp-action="Details" asp-route-id=@item.ProductId class="btn btn-primary">View Details</a>
                        <a href="#" class="btn btn-primary">Add to cart</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
