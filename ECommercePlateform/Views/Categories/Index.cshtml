@model IEnumerable<ECommercePlateform.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<a asp-controller="Categories" asp-action="Create" class="btn btn-primary">Add a New Category</a>
<div class="text-center mt-4">
    <h2>Categories</h2>
    <p>Select a category to view the products!</p>
</div>
@await Component.InvokeAsync("CategoryList")

@*
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Picture)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                Html.DisplayFor(modelItem => item.Picture)
                <img src="data:image;base64,@System.Convert.ToBase64String(item.Picture)" />
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@
